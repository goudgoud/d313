<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="siga.mireille.d228">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">

        <activity android:name="siga.mireille.d228.activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
              <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="siga.mireille.d228.activity.MainActivity">
            <intent-filter>
                <action android:name="siga.mireille.d228.MAINACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name="siga.mireille.d228.activity.SurveyFamilyActivity">

        </activity>

        <activity android:name="siga.mireille.d228.activity.SurveyActivity">

        </activity>

        <activity
            android:name="siga.mireille.d228.activity.MyPreferenceActivity"
            android:label="Preferences">

        </activity>

    </application>

</manifest>